import Vue from 'vue';

// 单元格
import {
  Cell,
  CellGroup
} from "vant";
Vue.use(Cell).use(CellGroup);

// 输入框
import {
  Field
} from "vant";
Vue.use(Field);

// 按钮
import {
  Button
} from "vant";
Vue.use(Button);

// 图标
import {
  Icon
} from "vant";
Vue.use(Icon);

// 九宫格
import{
  Grid,
  GridItem
} from "vant";
Vue.use(Grid).use(GridItem);
